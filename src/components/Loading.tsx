const Loading = () => {
  return (
    <span className="mx-auto flex items-center justify-center h-full">
      <span className="loading loading-dots w-[100px]"></span>
    </span>
  );
};

export default Loading;
